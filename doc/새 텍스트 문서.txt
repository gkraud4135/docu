Have to do
---------------------
메인페이지or개인페이지-버튼>게시물등록창>등록>게시물등록창>해당창종료>부모창리로딩(출력리로딩)

게시물출력 boardMapper에서 게시물가져오려면 갯, 현재는 출력시마다> 지우고 전체가져오고 식으로 되어있는데 전체게시물출력에선 필요한것만 가져와서 append식으로
	   사진가져오기 게시물삭제 


좋아요목록 >> 영웅이형
댓글 ///시간(추가해야함&날짜 jsp에서 변환되어야함), append라 div밖으로 튀어나감




Did
-------------------
메인창 자동로그인//해제

personpage수정중

loginMapper viewtimeupdate
나머진 보드dao관리자


drop table attach;
-- -----------------------------------------------------
-- Table `mvc`.`attach`
-- -----------------------------------------------------
CREATE TABLE IF NOT EXISTS `mvc3`.`attach` (
  `no` INT NOT NULL AUTO_INCREMENT,
  `name` VARCHAR(50) NOT NULL,
  `board_no` INT NOT NULL,
  `size` INT NOT NULL,
  PRIMARY KEY (`no`),
  INDEX `fk_attach_board1_idx` (`board_no` ASC),
  CONSTRAINT `fk_attach_board` FOREIGN KEY (`board_no`) REFERENCES `mvc3`.`board` (`no`) ON DELETE NO ACTION ON UPDATE NO ACTION
)ENGINE = InnoDB;

SET SQL_MODE=@OLD_SQL_MODE;
SET FOREIGN_KEY_CHECKS=@OLD_FOREIGN_KEY_CHECKS;
SET UNIQUE_CHECKS=@OLD_UNIQUE_CHECKS;

use project;
use mvc3;
select*from board;
select * from attach;

select*from member where sn =3;
select id from board where sn=3;
desc board;
desc attach;
select * from attach where sn=1;
desc boardlike;
select *from boardlike;
select count(*) from boardlike where board_sn=42;
select count(*) from boardlike where board_sn=43 and writer=3;
desc 게시물좋아요;
select * from boardlike where 'like'=null; 
select * from attach;
show tables;
select count(*) from comment where board_sn=43;
select count(*) from board LEFT JOIN friend ON board.writer = friend.my_sn join state on friend.friend_sn=state.sn where friend.friend_sn=3 and (board.state='전체' or board.state='친구') and wdate>lastviewtime order by wdate desc limit 3;
insert into comment(board_sn,writer,contents)values(378,3,"ddff32");
delete from comment where sn=41;
select * from attach join board on board.sn = attach.sn where board_sn = 53;
desc friend;
desc state;
insert into friend (my_sn,friend_sn,state)values(3,5,'친구');
select*from board;
select*from state;
update state set lastviewtime = viewtime where sn=3;
update state set viewtime=current_timestamp() where sn=3;
update board set wdate = viewtime where sn=5;
select*from friend;
insert into board(contents,writer,state)values("dsfds",7,"친구");
Insert into state(sn) values (sn);
insert into state(state,view,viewtime,lastviewtime)values("접속중","");
select *from board LEFT JOIN friend ON board.writer = friend.my_sn join state on friend.friend_sn=state.sn where friend.friend_sn=3 and (board.state='전체' or board.state='친구') and wdate<viewtime  order by wdate desc limit 50;
select *from board LEFT JOIN friend ON board.writer = friend.my_sn join state on friend.friend_sn=state.sn where friend.friend_sn=3 and (board.state='전체' or board.state='친구') and wdate>lastviewtime order by wdate desc limit 0 ,3;

select count(*) from board LEFT JOIN friend ON board.writer = friend.my_sn join state on friend.friend_sn=state.sn where friend.friend_sn=3 and (board.state='전체' or board.state='친구') and wdate>lastviewtime order by wdate desc limit 3;




